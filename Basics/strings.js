// two ways to declare string
let a = "dheeraj"
console.log(a)

const obj = new String("pandey")
console.log(obj[0])
console.log(obj.length)
console.log(obj.charAt(1))
console.log(obj.indexOf("a"))

// toUpperCase() - Converts the string to uppercase.
// toLowerCase() - Converts the string to lowercase.
// trim() - Removes whitespace from both ends of the string.
// trimStart() / trimEnd() - Removes whitespace from the start or end.
// replace(searchValue, replaceValue) - Replaces a substring with another value.
// replaceAll(searchValue, replaceValue) - Replaces all occurrences of a substring.